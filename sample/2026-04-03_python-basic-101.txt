<10:00:00> T1: 안녕하세요, 오늘은 파이썬을 처음 시작하는 분들을 위한 기초 문법 수업입니다.
<10:00:15> T1: 오늘 목표는 변수, 자료형, 조건문, 반복문, 함수가 각각 무엇인지 이해하고 직접 간단한 코드를 읽는 것입니다.
<10:00:30> T1: 지난 시간에는 개발 환경을 설치하고 print 함수로 화면에 문장을 출력해 봤죠.
<10:00:45> T1: 오늘은 그다음 단계로, 프로그램이 값을 저장하고 판단하고 반복하는 방법을 순서대로 살펴보겠습니다.
<10:01:00> T1: 먼저 변수부터 볼게요. 변수는 값을 잠시 보관하는 이름표라고 생각하시면 됩니다.
<10:01:15> T1: 예를 들어 age라는 변수에 20을 넣으면, 나중에 age를 다시 불러와서 계산에 사용할 수 있습니다.
<10:01:30> T1: 파이썬에서는 등호가 수학의 정답 표시라기보다 오른쪽 값을 왼쪽 이름에 할당한다는 의미로 쓰입니다.
<10:01:45> S1: 변수 이름에는 숫자를 써도 되나요?
<10:02:00> T1: 좋은 질문입니다. 변수 이름 맨 앞에는 숫자를 쓸 수 없지만, 중간이나 끝에는 숫자를 넣을 수 있습니다.
<10:02:15> T1: 그래서 student1은 가능하지만 1student는 문법 오류가 납니다.
<10:02:30> T1: 또 변수 이름은 의미가 드러나게 짓는 것이 중요합니다. x보다 total_score가 훨씬 읽기 쉽겠죠.
<10:02:45> T1: 여기서 첫 번째 핵심은 변수는 값을 담는 상자라기보다, 값을 가리키는 이름이라는 점입니다.
<10:03:00> T1: 이제 자료형으로 넘어가겠습니다. 자료형은 값이 어떤 종류인지 알려 주는 정보입니다.
<10:03:15> T1: 대표적으로 정수 int, 실수 float, 문자열 str, 참과 거짓을 나타내는 bool이 있습니다.
<10:03:30> T1: 예를 들어 3은 정수이고 3.14는 실수이며, hello는 문자열이고 True는 불리언입니다.
<10:03:45> T1: 파이썬은 type 함수를 통해 값의 자료형을 확인할 수 있습니다.
<10:04:00> S2: 숫자를 따옴표로 감싸면 숫자로 계산되나요?
<10:04:15> T1: 따옴표로 감싸면 문자열로 처리됩니다. 그래서 "10" + "20"은 30이 아니라 1020이 됩니다.
<10:04:30> T1: 반대로 int("10")처럼 형 변환을 하면 문자열을 정수로 바꿔 계산할 수 있습니다.
<10:04:45> T1: 지금 보시는 예제는 사용자 나이를 입력받아 내년 나이를 출력하는 코드입니다.
<10:05:00> T1: input 함수는 기본적으로 문자열을 반환하므로, 계산 전에 int로 바꾸는 과정이 꼭 필요합니다.
<10:05:15> T1: 이 부분에서 초보자가 가장 많이 하는 실수가 문자열과 숫자를 섞어서 연산하는 것입니다.
<10:05:30> T1: 여기까지 이해되시나요? 왜 형 변환이 필요한지 설명할 수 있으면 지금 흐름을 잘 따라오신 겁니다.
<10:05:45> S1: 네, 입력값이 문자로 들어오니까 계산 전에 숫자로 바꿔야 합니다.
<10:06:00> T1: 맞습니다. 바로 그 지점이 핵심입니다. 이제 여러 값을 한 번에 다루는 리스트를 보겠습니다.
<10:06:15> T1: 리스트는 순서가 있는 묶음입니다. 예를 들어 fruits = ["apple", "banana", "mango"]처럼 만들 수 있습니다.
<10:06:30> T1: 리스트에서는 인덱스를 사용해 특정 위치의 값을 꺼냅니다. 첫 번째 값의 인덱스는 1이 아니라 0입니다.
<10:06:45> T1: 따라서 fruits[0]은 apple이고 fruits[1]은 banana입니다.
<10:07:00> T1: 이 0부터 시작하는 규칙은 파이썬뿐 아니라 다른 언어에서도 자주 나오니 꼭 기억해 두세요.
<10:07:15> S2: 리스트 안의 값을 바꿀 수도 있나요?
<10:07:30> T1: 네, 가능합니다. fruits[1] = "grape"처럼 쓰면 두 번째 요소가 banana에서 grape로 바뀝니다.
<10:07:45> T1: append 메서드를 사용하면 맨 뒤에 새 값을 추가할 수도 있습니다.
<10:08:00> T1: 이제 반복문으로 연결해 보겠습니다. 리스트를 만들고 각 요소를 하나씩 출력하는 상황이 아주 자주 등장합니다.
<10:08:15> T1: for item in fruits라고 쓰면 리스트 안의 값을 차례대로 꺼내 item 변수에 담아 사용할 수 있습니다.
<10:08:30> T1: 이론 설명은 여기까지 하고, 지금부터는 짧은 실습 예제를 보겠습니다.
<10:08:45> T1: 과일 이름 앞에 번호를 붙여 출력하고 싶다면 enumerate를 쓰는 방식도 있지만, 오늘은 가장 기본 형태만 먼저 익히겠습니다.
<10:09:00> T1: 반복문에서 중요한 것은 들여쓰기입니다. 같은 블록에 속하는 문장은 같은 깊이로 맞춰야 합니다.
<10:09:15> T1: 파이썬은 중괄호 대신 들여쓰기로 구조를 구분하므로, 공백 하나만 어긋나도 에러가 날 수 있습니다.
<10:09:30> S3: 탭이랑 스페이스를 섞어 쓰면 안 되나요?
<10:09:45> T1: 가능할 때도 있지만 권장하지 않습니다. 협업에서는 스페이스 네 칸으로 통일하는 것이 가장 안전합니다.
<10:10:00> T1: 여기서 두 번째 핵심은 파이썬에서는 보기 좋은 코드가 곧 실행도 잘 되는 코드에 가깝다는 점입니다.
<10:10:15> T1: 이제 조건문으로 넘어가겠습니다. 조건문은 어떤 상황에서 다른 동작을 하게 만드는 문장입니다.
<10:10:30> T1: if score >= 80이라고 쓰면 점수가 80 이상일 때만 특정 코드가 실행됩니다.
<10:10:45> T1: 그렇지 않은 경우를 처리하고 싶으면 else를 붙이고, 여러 갈래로 나누고 싶으면 elif를 사용합니다.
<10:11:00> T1: 예를 들어 비가 오면 우산을 챙기고, 그렇지 않으면 그냥 나간다는 식으로 일상 판단을 코드로 옮기는 것입니다.
<10:11:15> T1: 이런 비유를 떠올리면 조건문은 훨씬 덜 추상적으로 느껴집니다.
<10:11:30> S1: 비교 연산자랑 등호가 같이 나오는 건 아직 좀 헷갈립니다.
<10:11:45> T1: 자연스러운 반응입니다. =는 할당이고, ==는 두 값이 같은지 비교하는 연산자입니다.
<10:12:00> T1: 그래서 score = 80은 값을 넣는 코드이고, score == 80은 같은지 검사하는 조건입니다.
<10:12:15> T1: 이 차이를 모르면 에러가 나거나 의도와 다른 결과가 나오니 꼭 구분해 주세요.
<10:12:30> T1: 이제 함수로 가겠습니다. 함수는 자주 쓰는 코드를 이름 붙여 묶어 두는 도구입니다.
<10:12:45> T1: def greet(name):처럼 만들고, 내부에 print 문을 넣으면 greet("민지")처럼 호출할 수 있습니다.
<10:13:00> T1: 함수의 장점은 반복을 줄이고, 같은 동작을 여러 곳에서 재사용할 수 있다는 점입니다.
<10:13:15> T1: 또 입력값인 매개변수와 결과값인 반환값을 구분해서 생각하면 함수를 더 쉽게 이해할 수 있습니다.
<10:13:30> T1: 예를 들어 add(a, b) 함수는 a와 b를 받아 더한 값을 return으로 돌려줄 수 있습니다.
<10:13:45> T1: 실무에서는 print만 하는 함수보다, 값을 반환하는 함수가 재사용성이 더 높습니다.
<10:14:00> S2: return이 없으면 함수가 끝나고 아무 값도 안 주는 건가요?
<10:14:15> T1: 맞습니다. 정확히는 None이라는 특별한 값이 반환됩니다.
<10:14:30> T1: 지금부터는 함수 예제를 한 번 더 보고, 조건문과 함께 쓰는 모습도 확인하겠습니다.
<10:14:45> T1: 사용자의 점수를 받아 합격 여부를 문자열로 돌려주는 함수를 만들면, 메인 코드가 훨씬 읽기 쉬워집니다.
<10:15:00> T1: 마지막으로 모듈과 예외 처리만 아주 가볍게 소개하겠습니다.
<10:15:15> T1: import math처럼 필요한 기능을 가져오면 제곱근 계산 같은 기능을 직접 만들지 않고 사용할 수 있습니다.
<10:15:30> T1: 그리고 try except는 실행 중 오류가 발생했을 때 프로그램이 바로 멈추지 않도록 도와줍니다.
<10:15:45> T1: 예를 들어 숫자가 아닌 값을 int로 바꾸려 하면 오류가 나는데, 이때 사용자에게 다시 입력하라고 안내할 수 있습니다.
<10:16:00> T1: 오늘 배운 흐름을 정리해 보면 변수로 값을 저장하고, 자료형을 이해한 뒤, 조건문과 반복문으로 흐름을 만들고, 함수를 통해 코드를 정리하는 것입니다.
<10:16:15> T1: 집에서는 사용자 이름과 점수를 입력받아 합격 여부를 알려 주는 짧은 프로그램을 직접 작성해 보세요.
<10:16:30> T1: 다음 시간에는 딕셔너리와 파일 입출력을 다루면서 데이터를 조금 더 구조적으로 관리해 보겠습니다.
<10:16:45> T1: 질문이 없다면 오늘 수업은 여기까지 하겠습니다. 수고 많으셨습니다.
